# .github/workflows/storage-ci-cd.yml
name: ampm-storage CI/CD (Dockerfile-based)

on:
  pull_request:
    branches: [ "main", "develop" ]
  push:
    branches: [ "main", "develop" ]

env:
  JAVA_VERSION: '21'
  TZ: 'Asia/Seoul'

jobs:
  # =======================================================
  # 1. CI Job (Pull Request 시 실행)
  # =======================================================
  ci:
    name: Continuous Integration
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build and Test with Gradle
        run: ./gradlew build

  # =========================================================================
  # 2. CD Job (Push(Merge) 시 실행) - 복사 스텝 분리 및 경로 수정
  # =========================================================================
  cd:
    name: Continuous Deployment
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    runs-on: ubuntu-latest
    steps:
      # (1) 소스 코드 가져오기
      - name: Checkout source code
        uses: actions/checkout@v4

      # (2) JDK 21 설정
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'

      # (3) Gradle 실행 권한 부여
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # (4) Gradle 빌드 (jar 파일 생성)
      - name: Build jar (skip tests)
        run: ./gradlew clean bootJar -x test

      # (5) 배포할 아티팩트 준비 (app.jar)
      - name: Prepare artifact for deployment
        shell: bash
        run: |
          JAR=$(find . -type f -path "*/build/libs/*.jar" | head -n 1)
          cp "$JAR" app.jar
          echo "Copied $JAR to app.jar"

      # (6) 브랜치에 따라 환경 변수 선택
      - name: Pick environment
        shell: bash
        run: |
          BRANCH="${GITHUB_REF##*/}"
          INTERNAL_PORT=6736
          INTERNAL_UPLOAD_DIR="/app/files"

          if [ "$BRANCH" = "main" ]; then
            {
              echo "APP_NAME=ampm-storage"
              echo "PROFILE=main"
              echo "HOST=${{ secrets.DEPLOY_HOST }}"
              echo "USER=${{ secrets.DEPLOY_USER }}"
              echo "PASSWORD=${{ secrets.DEPLOY_SSH_PASSWORD }}"
              echo "SSH_PORT=${{ secrets.DEPLOY_PORT }}"
              echo "DIR=ampm-storage"
              echo "STORAGE_BASE=${{ secrets.MAIN_STORAGE_BASE }}"
              echo "APP_PORT=6736"
              echo "APP_STORAGE_SECRET=${{ secrets.MAIN_APP_STORAGE_SECRET_KEY }}"
              echo "FRONTEND_DOMAIN=${{ secrets.MAIN_FRONTEND_DOMAIN }}"
          
              echo "ENV_SPRING_APP_NAME=AmpmStorage"
              echo "ENV_INTERNAL_PORT=$INTERNAL_PORT"
              echo "ENV_UPLOAD_DIR=$INTERNAL_UPLOAD_DIR"
              echo "ENV_MAX_FILE_SIZE=10MB"
              echo "ENV_MAX_REQ_SIZE=10MB"
            } >> $GITHUB_ENV
          elif [ "$BRANCH" = "develop" ]; then
            {
              echo "APP_NAME=ampm-storage-test"
              echo "PROFILE=test"
              echo "HOST=${{ secrets.TEST_DEPLOY_HOST }}"
              echo "USER=${{ secrets.TEST_DEPLOY_USER }}"
              echo "PASSWORD=${{ secrets.TEST_DEPLOY_SSH_PASSWORD }}"
              echo "SSH_PORT=${{ secrets.TEST_DEPLOY_PORT }}"
              echo "DIR=ampm-storage-test"
              echo "STORAGE_BASE=${{ secrets.TEST_STORAGE_BASE }}"
              echo "APP_PORT=6737"
              echo "APP_STORAGE_SECRET=${{ secrets.TEST_APP_STORAGE_SECRET_KEY }}"
              echo "FRONTEND_DOMAIN=${{ secrets.TEST_FRONTEND_DOMAIN }}"
          
              echo "ENV_SPRING_APP_NAME=AmpmStorage-Test"
              echo "ENV_INTERNAL_PORT=$INTERNAL_PORT"
              echo "ENV_UPLOAD_DIR=$INTERNAL_UPLOAD_DIR"
              echo "ENV_MAX_FILE_SIZE=10MB"
              echo "ENV_MAX_REQ_SIZE=10MB"
            } >> $GITHUB_ENV
          fi

      # (7) 원격 서버에 디렉터리 생성 (SSH 스크립트에서 $HOME 사용)
      - name: Make remote dirs
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USER }}
          password: ${{ env.PASSWORD }}
          port: ${{ env.SSH_PORT }}
          script: |
            # [수정] $HOME 변수를 사용하여 절대 경로 보장
            mkdir -p "$HOME/${{ env.DIR }}"
            mkdir -p "${{ env.STORAGE_BASE }}"

      # (8) 복사 전 GitHub Runner의 파일 목록 확인 (디버깅)
      - name: Check files in runner workspace
        shell: bash
        run: |
          echo "--- Verifying files in GitHub runner (before copy) ---"
          ls -l
          echo "------------------------------------------------------"

      # (9) app.jar와 Dockerfile만 서버로 복사
      - name: Copy app.jar to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USER }}
          password: ${{ env.PASSWORD }}
          port: ${{ env.SSH_PORT }}
          source: "app.jar"
          target: "$HOME/${{ env.DIR }}/" # [수정] $HOME 변수 사용

      - name: Copy Dockerfile to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USER }}
          password: ${{ env.PASSWORD }}
          port: ${{ env.SSH_PORT }}
          source: "Dockerfile"
          target: "$HOME/${{ env.DIR }}/" # [수정] $HOME 변수 사용

      # (10) 서버에서 .env 생성 및 도커 실행
      - name: Deploy on server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ env.HOST }}
          username: ${{ env.USER }}
          password: ${{ env.PASSWORD }}
          port: ${{ env.SSH_PORT }}
          envs: |
            APP_NAME, PROFILE, DIR, STORAGE_BASE, APP_PORT, 
            APP_STORAGE_SECRET, ENV_SPRING_APP_NAME, 
            ENV_INTERNAL_PORT, ENV_UPLOAD_DIR, 
            ENV_MAX_FILE_SIZE, ENV_MAX_REQ_SIZE, TZ,
            FRONTEND_DOMAIN

          script: |
            set -euo pipefail
            
            # (A) 앱 디렉토리(DIR)로 이동 (SSH 세션에서 $HOME 확장)
            echo "🚀 Starting deployment in $HOME/$DIR..."
            cd "$HOME/$DIR"

            # (B) 원격 서버의 파일 목록 확인 (디버깅)
            echo "--- Verifying files in $HOME/$DIR (before build) ---"
            pwd
            ls -l
            echo "----------------------------------------------"
            
            # (C) .env 파일 생성
            echo "Writing .env file..."
            cat > .env <<EOF
            TZ=$TZ
            SPRING_PROFILES_ACTIVE=$PROFILE
            SERVER_PORT=$ENV_INTERNAL_PORT
            SPRING_APPLICATION_NAME=$ENV_SPRING_APP_NAME
            APP_STORAGE_SECRET_KEY=$APP_STORAGE_SECRET
            APP_STORAGE_UPLOAD_DIR=$ENV_UPLOAD_DIR
            SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE=$ENV_MAX_FILE_SIZE
            SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE=$ENV_MAX_REQ_SIZE
            FRONTEND_DOMAIN=$FRONTEND_DOMAIN
            EOF

            # (D) Docker 이미지 빌드
            echo "🐳 Building new Docker image: $APP_NAME"
            docker build -t $APP_NAME .

            # (E) 기존 컨테이너 중지/삭제
            echo "🛑 Stopping and removing old container: $APP_NAME"
            docker stop $APP_NAME || true
            docker rm $APP_NAME || true

            # (F) 새 컨테이너 실행
            echo "▶️ Running new container: $APP_NAME"
            docker run -d \
              --name $APP_NAME \
              --restart unless-stopped \
              --env-file .env \
              -p "$APP_PORT:$ENV_INTERNAL_PORT" \
              -v "$STORAGE_BASE:$ENV_UPLOAD_DIR" \
              $APP_NAME:latest
            
            echo "🎉 Deployment successful!"